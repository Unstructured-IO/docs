```python Python SDK
# ...
from unstructured_client.models.shared import (
    <Create|Update>SourceConnector,
    SourceConnectorType,
    MongoDBConnectorConfigInput
)
# ...
source_connector = <Create|Update>SourceConnector(
    name="<name>", # Create only.
    type=SourceConnectorType.MONGODB, # Create only.
    config=MongoDBConnectorConfigInput(
        uri="<uri>",
        database="<database>",
        collection="<collection>"
    )
)
# ...
```