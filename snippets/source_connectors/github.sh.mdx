```bash Shell
#!/usr/bin/env bash

unstructured-ingest \
  github \
    --url Unstructured-IO/unstructured \
    --git-branch main \
    --output-dir $LOCAL_FILE_OUTPUT_DIR \
    --num-processes 2 \
    --verbose \
    --strategy hi_res
```
