```python Python SDK
# ...
from unstructured_client.models.shared import (
    <Create|Update>SourceConnector,
    SourceConnectorType,
    SnowflakeSourceSourceConnectorConfigInput
)
# ...
source_connector = <Create|Update>SourceConnector(
    name="<name>", # Create only.
    type=SourceConnectorType.SNOWFLAKE, # Create only.
    config=SnowflakeSourceConnectorConfigInput(
        account="<account>",
        role="<role>",
        user="<user>",
        password="<password>",
        host="<host",
        port=<port>
        database="<database>",
        schema_="<schema>",
        table_name="<table-name>",
        id_column="<id-column>",
        fields=[
            "<field>", 
            "<field>"
        ],
        batch_size=<batch-size> 
    )
)
# ...
```