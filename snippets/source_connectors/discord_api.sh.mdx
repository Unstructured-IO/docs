```bash Shell
#!/usr/bin/env bash

unstructured-ingest \
  discord \
    --channels 12345678 \
    --token $DISCORD_TOKEN \
    --download-dir $LOCAL_FILE_DOWNLOAD_DIR \
    --output-dir discord-example \
    --preserve-downloads \
    --verbose \
    --partition-by-api \
    --api-key $UNSTRUCTURED_API_KEY \
    --strategy hi_res
```
