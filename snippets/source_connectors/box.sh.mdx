```bash CLI
#!/usr/bin/env bash

unstructured-ingest \
  box \
    --box-app-config $BOX_APP_CONFIG_PATH \
    --remote-url $BOX_REMOTE_URL \
    --output-dir box-ingest-output \
    --num-processes 2 \
    --recursive \
    --verbose
```
