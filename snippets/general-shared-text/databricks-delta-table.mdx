- A [Databricks account]().
- A [Databricks workspace]() within the account.
- A Unity Catalog [catalog](), [schema](), and [table]() within the workspace.

  The table ...

- Optionally, a Unity Catalog [volume]() within the account. 

  ...

- A [Databricks SQL warehouse]() within the workspace.
- For Databricks personal access token authentication to the workspace, ...
- For Databricks OAuth machine-to-machine (M2M) authentication to the workspace, ...
- The following Databricks workspace permissions:

  - ...