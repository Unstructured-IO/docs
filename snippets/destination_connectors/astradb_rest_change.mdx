```bash REST (Change)
curl --request PUT \
"$UNSTRUCTURED_API_URL/destinations/<connector-id>" \
--header "unstructured-api-key: $UNSTRUCTURED_API_KEY" \
--data \
'{
    "type": "datastax_astra_db",
    "name": "<name>",
    "collection_name": "<collection-name>",
    "keyspace": "<keyspace>",
    "batch_size": <batch-size>,
    "api_endpoint": "<api-endpoint>",
    "token", "<token>"
}'
```