---
title: Document elements and metadata
---

import DocumentElements from '/snippets/concepts/document-elements.mdx';

<DocumentElements/>


### Extracting additional metadata with regex

You can extract additional metadata with regex using the `regex_metadata` kwarg. Here is an example:

```python
from unstructured.partition.text import partition_text

text = "SPEAKER 1: It is my turn to speak now!"
elements = partition_text(text=text, regex_metadata={"speaker": r"SPEAKER \d{1,3}:"})
elements[0].metadata.regex_metadata
```

The result will look like:

```json
{'speaker':
  [
    {
      'text': 'SPEAKER 1:',
      'start': 0,
      'end': 10,
   }
  ]
}
```
